const INITIAL_STATE = {
  token: '',
  player: {
    name: '',
    assertions: '',
    score: '',
    gravatarEmail: '',
  },
  players: [],
};

const testReducer = (state = INITIAL_STATE) => {
  switch ('a') {
  default:
    return state;
  }
};

export default testReducer;
